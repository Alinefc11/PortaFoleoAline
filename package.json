
{
  "scripts": {
        "sass": "sass src/scss:dist/css",
        "dev": "gulp",
        "build": "gulp build"
    },"keywords": [
        "SASS",
        "NMP",
        "Gulp"
    ],
  "devDependencies": {
    "gulp": "^5.0.1",
    "gulp-sass": "^6.0.1",
    "sass": "^1.98.0"
  }
}
